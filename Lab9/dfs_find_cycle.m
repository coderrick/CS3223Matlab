function res = dfs_find_cycle(edges, u)
%{
colors[u] = gray, 1 ?
visited[u] = 1;
for i=1:n
 if (visited(i)==0)
         dfs_find_cycle(edges, i);
  else
     colors[v] == 'gray'
            find a cycle
  end  
 end
colors[u] = black, 2, done.

%}